using System;
using System.IO;

namespace FileOP;

internal static class Program
{
    private static void Main(string[] args)
    {
        //Parse user arguments passed into the application
        Fiop.ParseArguments(args);
    }
}